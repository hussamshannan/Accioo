const presenceHandlers = (io, socket, rooms) => {
  // Online presence is handled by connect/disconnect events in index.js
  // This file is a placeholder for future presence features:
  // - Last seen tracking
  // - User status (away, busy, etc.)
  // - Presence broadcast to friends
};

module.exports = presenceHandlers;
